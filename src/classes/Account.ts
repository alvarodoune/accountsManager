export class Account {
      public title: string;
      public name: string;
      public icon: string;

      constructor(title: string = "", name: string = "", icon: string = "") {
            this.title = title;
            this.name = name;
            this.icon = icon;
      }
}
