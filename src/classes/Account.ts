export class Account {
      public title: String;
      public icon: String;

      constructor(title: String, icon: String) {
            this.title = title;
            this.icon = icon;
      }
}
